startCommand:
  type: http
  command:
    - node
    - dist/index.js
    - --transport
    - streamable-http
    - --port=${PORT}

configSchema:
  type: object
  properties:
    port:
      type: integer
      default: 3000

exampleConfig:
  port: 3000
