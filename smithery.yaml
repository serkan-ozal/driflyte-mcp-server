runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      port:
        type: integer
        default: 8081
  exampleConfig:
    port: 8081
